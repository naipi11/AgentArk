{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only the default core capability is enabled.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
